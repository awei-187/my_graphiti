# Grpahiti 配置（以neo4j为例）及运行文档

### 一、代码仓库：

https://github.com/awei-187/my_graphiti.git

### 二、配置环境：

- Python 3.10 or higher
- Neo4j
- OpenAI API key
- graphiti：

```bash
pip install graphiti-core
```

#### 不妨先测试一下 quickstart 程序能否正常运行

转到 /examples/quickstart 文件夹下：

**（Neo4j应已安装并正在运行一个本地实例，且该实例密码应与以下设置的环境变量中的 NEO4J_PASSWORD 相同）**

设置环境变量（.env例子）：

```bash
OPENAI_API_KEY='xxx'
OPENAI_BASE_URL='xxx'

# Neo4j database connection
NEO4J_URI='bolt://localhost:7687'
NEO4J_PORT=
NEO4J_USER='neo4j'
NEO4J_PASSWORD='xxx'

# FalkorDB database connection 
FALKORDB_URI=
FALKORDB_PORT=
FALKORDB_USER=
FALKORDB_PASSWORD=

USE_PARALLEL_RUNTIME=
SEMAPHORE_LIMIT=
GITHUB_SHA=
MAX_REFLEXION_ITERATIONS=
ANTHROPIC_API_KEY=
```

（'xxx'为需要配置项、已填好的为 neo4j 的默认配置，根据实际情况修改，其余可不填写）

再运行（）：

```bash
python quickstart_neo4j.py
```

能成功运行即可。

### 三、Graphiti 的具体使用样例

#### 添加剧集（https://help.getzep.com/graphiti/core-concepts/adding-episodes）

支持的剧集类型：text、message、json

**Adding a `text` or `message` Episode：**

```python
await graphiti.add_episode(
    name="tech_innovation_article",
    episode_body=(
        "MIT researchers have unveiled 'ClimateNet', an AI system capable of predicting "
        "climate patterns with unprecedented accuracy. Early tests show it can forecast "
        "major weather events up to three weeks in advance, potentially revolutionizing "
        "disaster preparedness and agricultural planning."
    ),
    source=EpisodeType.text,
    # A description of the source (e.g., "podcast", "news article")
    source_description="Technology magazine article",
    # The timestamp for when this episode occurred or was created
    reference_time=datetime(2023, 11, 15, 9, 30),
)
```

```python
await graphiti.add_episode(
    name="Customer_Support_Interaction_1",
    episode_body=(
        "Customer: Hi, I'm having trouble with my Allbirds shoes. "
        "The sole is coming off after only 2 months of use.\n"
        "Support: I'm sorry to hear that. Can you please provide your order number?"
    ),
    source=EpisodeType.message,
    source_description="Customer support chat",
    reference_time=datetime(2024, 3, 15, 14, 45),
)
```

**Adding an Episode using structured data in JSON format：**

```python
product_data = {
    "id": "PROD001",
    "name": "Men's SuperLight Wool Runners",
    "color": "Dark Grey",
    "sole_color": "Medium Grey",
    "material": "Wool",
    "technology": "SuperLight Foam",
    "price": 125.00,
    "in_stock": True,
    "last_updated": "2024-03-15T10:30:00Z"
}

# Add the episode to the graph
await graphiti.add_episode(
    name="Product Update - PROD001",
    episode_body=product_data,  # Pass the Python dictionary directly
    source=EpisodeType.json,
    source_description="Allbirds product catalog update",
    reference_time=datetime.now(),
)
```

#### 搜索（https://help.getzep.com/graphiti/working-with-data/searching）

Graphiti 库中的两种搜索方法：

**基本搜索（混合搜索）：**（返回边，UUID、fact等）

```python
await graphiti.search(query)
```

结合**语义相似度**（嵌入向量相似度）和 **BM25 检索**（改进版 TF-IDF ），使用**倒数秩融合**重新排序。

**中心节点搜索（节点距离重新排序）：**（返回边，UUID、fact等）

```python
await graphiti.search(query, center_node_uuid)
```

通过根据与图中指定节点的接近程度对结果进行优先排序来**扩展上述混合搜索**。

**使用搜索配方进行节点搜索：**（返回节点，UUID、name、summary等）

**以及其他可配置的搜索策略**

### 四、一些已完成的 Graphiti 测试代码（在 /my_evals 文件夹下）

#### Locomo_test

仿照 Zep 的 Locomo_test 代码：https://github.com/getzep/zep-papers/tree/main/kg_architecture_agent_memory/locomo_eval 而编写。

按照 ingestion -> search -> responses -> eval 的顺序运行即可。

