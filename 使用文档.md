# Grpahiti 配置（基于neo4j）及运行文档

### 一、代码仓库：

https://github.com/awei-187/my_graphiti.git

### 二、配置环境：

- Python 3.10 or higher
- Neo4j
- OpenAI API key
- graphiti：

```bash
pip install graphiti-core
```

#### 不妨先测试一下 quickstart 程序能否正常运行

转到 /examples/quickstart 文件夹下：

**（Neo4j应已安装并正在运行一个本地实例，且该实例密码应与以下设置的环境变量中的 NEO4J_PASSWORD 相同）**

设置环境变量（.env例子）：

```bash
OPENAI_API_KEY='xxx'
OPENAI_BASE_URL='xxx'

# Neo4j database connection
NEO4J_URI='bolt://localhost:7687'
NEO4J_PORT=
NEO4J_USER='neo4j'
NEO4J_PASSWORD='xxx'

USE_PARALLEL_RUNTIME=
SEMAPHORE_LIMIT=
GITHUB_SHA=
MAX_REFLEXION_ITERATIONS=
```

（'xxx'为需要配置项、已填好的为 neo4j 的默认配置，根据实际情况修改，其余可不填写）

再运行（）：

```bash
python quickstart_neo4j.py
```

能成功运行即可。

### 三、一些 Graphiti 评测代码（在 /my_evals 文件夹下）

#### Locomo_test

为仿照 Zep 的 Locomo_test 代码：https://github.com/getzep/zep-papers/tree/main/kg_architecture_agent_memory/locomo_eval 而编写。

同样设置环境变量、运行Neo4j实例后按照 ingestion -> search -> responses -> eval 的顺序运行四个python程序即可。

